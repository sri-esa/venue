export const readRTDB = async (path: string): Promise<any> => {
    // Returns Mock Object
    return {};
}

export const waitForRTDBValue = async (path: string, expectedValue: any, timeoutMs: number) => {
    return true;
}

export const clearRTDBPath = async (path: string): Promise<void> => {
    return;
}

export const seedRTDB = async (path: string, data: any): Promise<void> => {
    return;
}
