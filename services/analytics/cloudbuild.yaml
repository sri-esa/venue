steps:
  - name: 'gcr.io/cloud-builders/docker'
    id: 'build'
    args:
      - 'build'
      - '--file=analytics/Dockerfile'
      - '--tag=gcr.io/$PROJECT_ID/analytics:$SHORT_SHA'
      - '--tag=gcr.io/$PROJECT_ID/analytics:latest'
      - '.'

  - name: 'gcr.io/cloud-builders/docker'
    id: 'push'
    args: ['push', '--all-tags', 'gcr.io/$PROJECT_ID/analytics']
    waitFor: ['build']

  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    id: 'deploy'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'analytics'
      - '--image=gcr.io/$PROJECT_ID/analytics:$SHORT_SHA'
      - '--region=us-central1'
      - '--platform=managed'
      - '--allow-unauthenticated'
      - '--update-env-vars=PROJECT_ID=$PROJECT_ID,NODE_ENV=production'
      - '--memory=512Mi'
      - '--cpu=1'
      - '--max-instances=3'
      - '--port=8083'
    waitFor: ['push']

images:
  - 'gcr.io/$PROJECT_ID/analytics:$SHORT_SHA'
  - 'gcr.io/$PROJECT_ID/analytics:latest'

options:
  logging: CLOUD_LOGGING_ONLY
  substitutionOption: ALLOW_LOOSE
